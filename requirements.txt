# Core Django framework
Django==5.2.6

# Django REST Framework for API development
djangorestframework==3.16.1

# JWT authentication for DRF
djangorestframework-simplejwt==5.5.1

# Database drivers
psycopg2-binary==2.9.10

# Core dependencies (automatically installed with Django but pinned for consistency)
asgiref==3.9.1
sqlparse==0.5.3
tzdata==2025.2

# JWT handling
PyJWT==2.10.1

# CORS handling for API requests (recommended for frontend integration)
django-cors-headers==4.7.0

# Better form rendering (optional but useful)
django-crispy-forms==2.4

# Optional: For handling images if you use ImageField
# Pillow==10.4.0

# Development dependencies (uncomment for development)
# python-decouple==3.8
# django-extensions==3.2.3
